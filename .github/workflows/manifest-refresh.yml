name: Refresh Bungie Manifest

on:
  schedule:
    # Tuesday at 3:17 PM CDT / 2:17 PM CST, after weekly reset and typical maintenance.
    - cron: "17 20 * * 2"
  workflow_dispatch:

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.7.0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pnpm install --frozen-lockfile
      - run: pnpm manifest:sync
      - run: pnpm ratings:sync
      - run: pnpm --filter @guardian-nexus/web build
      - run: pnpm --filter @guardian-nexus/web run deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
