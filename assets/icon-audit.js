(() => {
  // Temporarily disabled: the strict icon audit scans the full Bungie manifest on page load.
  // That was too heavy for the live GitHub Pages app and could freeze loading.
  window.D2_COLLECTIONS_ICON_AUDIT_DISABLED = true;
})();
